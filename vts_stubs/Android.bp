// Stub defaults and module definitions for minimal TWRP builds.
// Several projects are removed from the minimal manifest, but many Android.bp
// files in remaining projects still reference modules from those removed projects.
// These empty stubs satisfy those references.

cc_defaults {
    name: "VtsHalDriverDefaults",
    // Intentionally empty — stub to satisfy build dependencies
}

cc_defaults {
    name: "VtsHalProfilerDefaults",
    // Intentionally empty — stub to satisfy build dependencies
}

cc_defaults {
    name: "vts_proto_fuzzer_default",
    // Intentionally empty — stub to satisfy build dependencies
}

cc_defaults {
    name: "cts_defaults",
    // Intentionally empty — stub to satisfy build dependencies
}

// frameworks/av stubs
cc_defaults {
    name: "libcodec2-internal-defaults",
    // Intentionally empty — stub to satisfy build dependencies
}

cc_defaults {
    name: "libcodec2-impl-defaults",
    // Intentionally empty — stub to satisfy build dependencies
}

cc_defaults {
    name: "libmedia_codecs_defaults",
    // Intentionally empty — stub to satisfy build dependencies
}

cc_defaults {
    name: "libaudioclient_defaults",
    // Intentionally empty — stub to satisfy build dependencies
}

java_defaults {
    name: "libnetworkstackutilsjni_deps",
    // Intentionally empty — stub to satisfy build dependencies
}

// Qualcomm camera stubs (hardware/qcom/camera removed from minimal manifest)
cc_defaults {
    name: "qti_camera_device_defaults",
    // Intentionally empty — stub to satisfy build dependencies
}

// VTS test defaults (test/vts removed from minimal manifest)
cc_defaults {
    name: "vts_target_tests_defaults",
    // Intentionally empty — stub to satisfy build dependencies
}

// external/okhttp stubs (removed from minimal manifest)
java_library {
    name: "okhttp-tests-nojarjar",
    // Intentionally empty — stub to satisfy build dependencies
}
