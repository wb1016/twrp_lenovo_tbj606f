// Stub defaults and module definitions for minimal TWRP builds.
// Several projects are removed from the minimal manifest, but many Android.bp
// files in remaining projects still reference modules from those removed projects.
// These empty stubs satisfy those references.

cc_defaults {
    name: "VtsHalDriverDefaults",
    // Intentionally empty — stub to satisfy build dependencies
}

cc_defaults {
    name: "VtsHalProfilerDefaults",
    // Intentionally empty — stub to satisfy build dependencies
}

cc_defaults {
    name: "VtsHalTargetTestDefaults",
    // Intentionally empty — stub to satisfy build dependencies
}

cc_defaults {
    name: "vts_proto_fuzzer_default",
    // Intentionally empty — stub to satisfy build dependencies
}

cc_defaults {
    name: "cts_defaults",
    // Intentionally empty — stub to satisfy build dependencies
}

cc_defaults {
    name: "hidl_defaults",
    // Intentionally empty — stub to satisfy build dependencies
    // (system/libhidl removed from minimal manifest)
}

cc_defaults {
    name: "libcodec2-internal-defaults",
    // Intentionally empty — stub to satisfy build dependencies
    // (frameworks/av removed from minimal manifest)
}

java_library {
    name: "libnetworkstackutilsjni_deps",
    // Intentionally empty — stub to satisfy build dependencies
}